export default (emails) => {
  const emailsArray = emails.split(",").map((email) => email.trim()).filter(email => );

  if(invalidEmails.length){
      return `These emails are invalid: ${invalidEmails}`;
  }


  return;
};
